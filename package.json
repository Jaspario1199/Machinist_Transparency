{
  "name": "machinist-transparency",
  "version": "0.2.0",
  "private": true,
  "description": "CNC manufacturing visibility layer — simulated beta and configuration contract",
  "type": "module",
  "scripts": {
    "build:config": "node scripts/build-demo-config.mjs",
    "test": "node --test \"tests/*.test.mjs\"",
    "serve": "python3 -m http.server 8080 --directory demo"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  },
  "engines": {
    "node": ">=20"
  }
}
